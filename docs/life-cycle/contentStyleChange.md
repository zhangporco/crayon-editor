# contentStyleChange(target, mutation)

内容区样式发生变化

@params target: 内容区dom
@params mutation: mutation 对象

```javascript
// test plugin
export default {
  id: 'demo',
  name: '',
  display: true,
  contentStyleChange: (target, mutation) => {}
}
```